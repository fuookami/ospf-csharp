#nullable enable

using Xunit;

namespace Fuookami.Ospf.Utils.Tests;

/// <summary>
/// Placeholder test to ensure the scaffold builds and the test runner finds at least one test.
/// Replaced by real tests in phase 2.
/// </summary>
public class ScaffoldBuilds
{
    [Fact]
    public void Scaffold_ShouldBuild_AndTestRunnerFindsTest()
    {
        // Placeholder — real tests land in phase 2 (error + result + functional).
        Assert.True(true);
    }
}
