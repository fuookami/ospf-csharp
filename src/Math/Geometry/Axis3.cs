#nullable enable

namespace Fuookami.Ospf.Math.Geometry;

/// <summary>三维坐标轴 / 3D axis enum (X, Y, Z).</summary>
public enum Axis3 {
    /// <summary>X 轴 / X axis.</summary>
    X,
    /// <summary>Y 轴 / Y axis.</summary>
    Y,
    /// <summary>Z 轴 / Z axis.</summary>
    Z,
}
